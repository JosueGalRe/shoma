[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.leyline"

[deploy]
healthcheckPath = "/health/protocol"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
