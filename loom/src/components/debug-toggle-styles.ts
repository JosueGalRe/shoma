import { tv } from 'tailwind-variants'

export const debugToggleStyles = tv({
  slots: {
    button:
      'border-lol-border-subtle/50 text-lol-text-muted hover:border-lol-border-gold hover:text-lol-text-primary focus-visible:ring-lol-border-gold min-h-[44px] rounded-sm border px-2 py-1 text-[10px] font-medium transition-colors focus-visible:ring-1 focus-visible:outline-none',
  },
})
