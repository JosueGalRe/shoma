{
  "name": "mimic-mono",
  "private": true,
  "packageManager": "bun@1.1.38",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "dev:rift-next": "bun run --filter @mimic/rift-next dev",
    "dev:web-next": "bun run --filter @mimic/web-next dev"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "shadcn": "^3.8.5"
  }
}
