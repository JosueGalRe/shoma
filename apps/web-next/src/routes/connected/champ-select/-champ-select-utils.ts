import type { ChampSelectRouteCopy } from './-champ-select-types'

export function readChampSelectRouteCopy(): ChampSelectRouteCopy {
  return {
    title: 'Champ-select route extracted',
    body: 'Champ-select controls are currently available in the lobby route while route-level split is in progress.',
    cta: 'Open Lobby',
  }
}
