Task 6 GREEN verification — semantic token CSS

Implemented:
- packages/design-system/src/tokens/semantic.css defines all 10 concrete --shoma-* semantic tokens for the RED token and contrast tests.
- packages/design-system/src/styles/tokens.css defines the same semantic token CSS custom properties for consumer imports.
- packages/design-system/src/styles/theme.css imports ./tokens.css and maps every semantic token through Tailwind v4 @theme --color-* entries.
- packages/design-system/src/index.ts exports designSystemThemeCss and designSystemTokensCss import strings.
- packages/design-system/tsconfig.json enables jsx: react-jsx so the existing TSX icon export can build.

Verification run 2026-05-13:
- lsp_diagnostics packages/design-system/src/index.ts: No diagnostics found.
- lsp_diagnostics packages/design-system/src/components/icon.tsx: No diagnostics found.
- lsp_diagnostics for CSS/JSON files: blocked because configured Biome LSP is not installed in this environment.
- bun run --filter @shoma/design-system test: exited 0; 40 pass, 0 fail, 134 expect() calls across 4 files.
- bun run --filter @shoma/design-system build: exited 0.

Contrast outcome:
- Existing contrast.test.ts passed through the package test command.
- foreground/background, primary-foreground/primary, muted/background, accent/background, and destructive/background all meet the asserted WCAG AA thresholds.
