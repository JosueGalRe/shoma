Task 6 CSS import validation

Validated with Bun script from repository root:
- packages/design-system/src/tokens/semantic.css contains --shoma-background, --shoma-foreground, --shoma-primary, --shoma-primary-foreground, --shoma-secondary, --shoma-accent, --shoma-muted, --shoma-border, --shoma-ring, and --shoma-destructive.
- packages/design-system/src/styles/tokens.css contains the same 10 --shoma-* semantic CSS custom properties.
- packages/design-system/src/styles/theme.css imports ./tokens.css.
- packages/design-system/src/styles/theme.css maps every token to Tailwind v4 @theme color values with --color-{token}: var(--shoma-{token}).

Command result:
CSS token import validation passed for 10 semantic tokens

Note:
The validation command emitted the existing root tsconfig warning about @shoma/protocol-contract using a non-relative path without baseUrl; the CSS validation itself exited 0.
