- 2026-05-14: LoL Client semantic palette now lives identically in `src/tokens/semantic.css` and `src/styles/tokens.css`; `semantic.css` remains the direct source for token-contract and contrast tests.
- 2026-05-14: Removing semantic token names breaks typed consumers immediately (`Icon` default tone and icon test needed `foreground` -> `text`).
- 2026-05-14: Design-system typography now owns the LoL Client font faces: Beaufort for LoL (400/700/900) for display and Spiegel (400/600/700) for body, loaded from CommunityDragon in `src/styles/typography.css`.
- 2026-05-14: `src/styles/theme.css` should keep stable Tailwind v4 utility names (`background`, `primary`, `secondary`, `muted`, etc.) while mapping them to the new LoL semantic tokens (`--shoma-surface`, `--shoma-text`, `--shoma-surface-elevated`, `--shoma-text-muted`, `--shoma-error`, `--shoma-success`).
- 2026-05-14: Badge variants now use the semantic token set directly (`primary`, `surface`, `text-muted`, `border`, `error`) while keeping the CVA variant names unchanged.
- Button component updated to use glassmorphism tokens (bg-surface/80, backdrop-blur-md, border-border-gold/30, shadow-[0_0_15px_color-mix(in_srgb,var(--shoma-primary)_30%,transparent)]).
- Input component updated to use glassmorphism tokens (bg-surface/40, text-text, border-primary focus rings) while preserving layout and file variants.
- Card component updated to use glassmorphism styles with new design system tokens (bg-surface/60, backdrop-blur-lg, border-border-gold/20).
- Alert component updated to use glassmorphism styling with new tokens (bg-surface/70, border-border-gold/30, bg-error/10, border-error/30).
- 2026-05-14: `packages/design-system/tests/contrast.test.ts` now asserts the LoL Client semantic palette values from `semantic.css` before checking WCAG ratios: text/surface AAA, primary/surface AA, accent/surface AA, and border/surface intentionally below AA for text.
- 2026-05-14: `loom/src/styles.css` now only keeps shared imports, Loom spacing tokens, and the base reset; the duplicate font faces and palette/shadow tokens were removed so the design system is the single source of truth.
- 2026-05-14: `lsp_diagnostics` could not run in this environment because Biome is not installed, so CSS verification relied on direct file inspection and the targeted grep check.
- 2026-05-14: Loom’s `styles.css` cleanup keeps `@import 'tailwindcss'`, the design-system theme import, `animations.css`, `:root { color-scheme: dark; }`, `body { margin: 0; }`, and the `--spacing-lol-space-*` tokens; all local `@font-face`, Google Fonts, and `--color-lol-*` / `--color-shoma-*` duplication stays out of the app file.
