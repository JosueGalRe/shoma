## 2026-05-12 - LCU champ-select Sona parity

- `web/src/core/lcu/parsers/champ-select.ts` keeps LCU backward compatibility by adding Sona-derived fields with `v.fallback(v.optional(...), undefined)` instead of required keys.
- Champ-select trade parsing should brand `cellId` with `CellIdSchema`, keep `id` as `finiteNumber`, and restrict `state` to `INVALID | AVAILABLE | BUSY | RECEIVED | SENT`.
- Web package verification for parser-only schema changes: `bun test tests/unit/lcu-parsers/champ-select.test.ts` and `bun run build` from `web/`.

## 2026-05-12 - T2 AssetResolver

- Added `web/src/lib/asset-resolver.ts` as pure functions over cached `ChampionSummary[]`, `SummonerSpellData[]`, and `RuneTree[]`; no hooks, no fetches, no module-level asset loading.
- Resolver indexes use WeakMap keyed by the supplied arrays, preserving caller-owned cache boundaries while avoiding repeated scans.
- Focused verification: `bun test src/lib/asset-resolver.test.ts`; build verification: `bun run build` from `web/`.

## 2026-05-12 - T5 fuzzy search utility

- `ChampionSummary.key` is the Data Dragon alias/string id in Mimic; champion search should rank name exact/startsWith/includes first, then title/key includes.
- `SummonerSpellData` is re-exported from `web/src/lib/asset-resolver.ts` and maps to `SummonerSpell` from LCU queries; spell search only uses `name`.
- Focused verification: `bun test web/src/lib/fuzzy-search.test.ts`; per-file lint works via `bunx vp lint --max-warnings=0 src/lib/fuzzy-search.ts src/lib/fuzzy-search.test.ts` from `web/`.

- 2026-05-12 F3 integration QA: `bun test src/lib/asset-resolver.test.ts src/lib/fuzzy-search.test.ts src/lib/lcu-normalizers.test.ts src/lib/deduped-query.test.ts` passed 25 tests across 4 files; evidence saved to `.sisyphus/evidence/step-0-f3-integration.log`.

## 2026-05-12 - T1 Playwright mobile baselines

- Added Mobile-360 and Mobile-390 Playwright projects with mobile/touch settings.
- Screenshot baselines are generated by web/tests/e2e/screenshots.pw.ts into web/tests/e2e/baselines/{screen}-{viewport}.png.
- The screenshot spec seeds sticky lobby sessionStorage and uses the dev \_\_mimicMockLcu bridge for champ-select, ready-check, and gameflow state.
- T1: Refactored members.tsx to use ChampionIdentity, added pick intent visuals, role icons, and shield overlay.

## 2026-05-12 - Plan 05 F3 Real Manual QA

- comparison.pw.ts --project=Mobile-360: PASS, 1/1 tests passed, exit 0.
- comparison.pw.ts --project=Mobile-390: PASS, 1/1 tests passed, exit 0.
- interactions.pw.ts --project=Mobile-360: PASS, 6/6 tests passed, exit 0.
- a11y.pw.ts --project=Mobile-360: PASS, 2/2 tests passed, exit 0.
- Verdict: APPROVE. No unexpected E2E failures observed.

## 2026-05-12 Plan 05 F4 scope fidelity re-evaluation

- Re-ran scope check with `git diff --name-only HEAD`; tracked working-tree changes are limited to workflow notes/plan evidence, dependency lock/package updates for `axe-core`, Playwright config, a11y-focused UI fixes, select-removal components, and connected route touch target fix.
- `git status --short` shows the additional untracked files are Plan 05 evidence artifacts and new `web/tests/e2e/*` coverage/harness files. No current working-tree changes appear under `web/src/core/`, `web/src/lib/`, or `web/src/testing/`.
- Corrected scope verdict: APPROVE; prior rejection based on branch comparison would conflate earlier in-scope commits with current Plan 05 working-tree changes.

## 2026-05-12 Plan 05 F2 re-evaluation

- Re-ran code-quality review with clarifications: `web/src/routes/connected/lobby/route.tsx` has empty `git diff HEAD -- ...` and is not in `git diff --name-only HEAD`.
- Changed TS/TSX files reviewed in current diff have clean LSP diagnostics and no introduced `as any`, `@ts-ignore`, empty catch, or `console.log` patterns.
- React Doctor threshold is 75 and `bun run doctor:react:check` passed with web:84, conduit:100.
- Web build passed. Web tests and lint still report existing failures outside the reviewed diff; not used as F2 rejection reasons under clarified scope.
- F2 verdict: APPROVE for clarified code-quality gate.
- Created PremadeReadyCheckOverlay for Swiftplay premade party ready checks.
- Used SVG circle with stroke-dasharray and stroke-dashoffset for the circular progress ring.
- Integrated into LobbyRouteComponent, gated by isSwiftplay and premade.isActive.
