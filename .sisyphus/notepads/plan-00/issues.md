## 2026-05-12 - T3 verification blockers

- `bun run build` is blocked by existing unrelated failures: `rift/tests/unit/env-config.test.ts` redeclares `loadConfig`, Conduit/Tauri fails on Linux with missing autogenerated permission files and `irelia` OS-gated process constants, and `@mimic/web` build fails on unused `@ts-expect-error` directives in existing test files.
- `bun run --filter '@mimic/web' lint` is blocked by existing unrelated lint/type issues in lobby/ready-check/testing files and root tsconfig parsing; targeted lint for `web/src/core/http/ddragon-client.ts` passes.
- JSON LSP diagnostics could not run because the configured `biome` language server is not installed in this environment; `jq empty .sisyphus/evidence/plan-00-t3-spells.json` validates the evidence file syntax.

## 2026-05-12 - T3 follow-up verification

- After correcting the hook to use champion keys, `bun run --filter '@mimic/web' build` passes. The full root `bun run build` remains blocked only by unrelated `@mimic/rift` duplicate `loadConfig` declarations and Conduit/Tauri Linux build errors.
- LSP diagnostics for `web/tests/unit/ddragon-client.test.ts` report missing `bun:test` types in the editor server, but `bun test web/tests/unit/ddragon-client.test.ts` runs successfully with 5 passing tests.
